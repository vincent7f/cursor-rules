---
description: 处理 [UI] 类 GitHub Issue 时须通过 MCP 运行游戏并验证界面后再提交
alwaysApply: true
---

# GitHub Issue [UI] 验证流程

当处理 **GitHub 上分类为 [UI] 的 Issue** 时（例如执行 `/go` 或按 issue 标题前缀识别），必须遵守以下流程，**不得在未通过验证的情况下提交到 Git**。

## 流程要求

1. **完成代码修改**  
   按 Issue 描述完成所有 UI 相关修改（布局、样式、连接线、地图高度等）。

2. **通过 MCP 运行游戏**  
   使用项目配置的 MCP 工具启动或确保游戏在运行（例如通过 RemoteAPI/游戏 WebSocket 或相关 MCP 能力）。

3. **读取并验证游戏界面**  
   通过 MCP 获取游戏界面（如截图、界面状态或可见区域），确认：
   - 修改已正确呈现在游戏中；
   - 效果符合 Issue 中的要求（例如连接线到边缘、虚线、地图高度不遮挡等）。

4. **通过验证后再提交**  
   仅当上述检查通过后，才执行 `git add` 与 `git commit`。若验证未通过，继续修改并重复步骤 2–3，直至满足要求再提交。

## 说明

- 本规则仅针对 **标题或标签为 [UI] 的 Issue**；[Bug]/[Feature]/[Todo] 等仍按原有流程（修改 → 自检/测试 → 提交）。
- 若当前环境无法通过 MCP 运行游戏或获取界面（例如 MCP 未配置、游戏未启动），应在回复中说明限制，并建议在可验证环境再合并或二次确认。
